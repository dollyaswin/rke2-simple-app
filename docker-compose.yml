version: '3.8'
services:
    k3app:
        image: 'python:3.12.1-alpine'
        container_name: api
        build:
           context: ./api
           dockerfile: Dockerfile
        env_file: .env
        ports:
            - '${API_PORT:-8080}:8080'
        restart: unless-stopped
